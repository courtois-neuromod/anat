name: Test deploy
on:
  push:
    branches: [ main, git-annex, tests]
  pull_request:
    branches: [ main ]
jobs:
  deploy_linux:
    uses: courtois-neuromod/actions-template/.github/workflows/deploy.yml@ed678a7547db6068d78dd98903cbf1b06160ac5b
    with:
      name: CNeuromod Bot
      email: courtois.neuromod@gmail.com
    secrets:
      ssh_key: ${{ secrets.CNEUROMOD_BOT_SSH_KEY }}
  deploy_mac:
    uses: courtois-neuromod/actions-template/.github/workflows/deploy_mac.yml@ed678a7547db6068d78dd98903cbf1b06160ac5b
    with:
      name: CNeuromod Bot
      email: courtois.neuromod@gmail.com
    secrets:
      ssh_key: ${{ secrets.CNEUROMOD_BOT_SSH_KEY }}
