name: Test deploy
on:
  push:
    branches: [ main, git-annex, tests]
  pull_request:
    branches: [ main ]
jobs:
  deploy_linux:
    uses: courtois-neuromod/actions-template/.github/workflows/deploy.yml@e17e9a6e8e62fffda1cbe08ce590b8095f8d4e24
    with:
      name: CNeuromod Bot
      email: courtois.neuromod@gmail.com
    secrets:
      ssh_key: ${{ secrets.CNEUROMOD_BOT_SSH_KEY }}
  deploy_mac:
    uses: courtois-neuromod/actions-template/.github/workflows/deploy_mac.yml@e17e9a6e8e62fffda1cbe08ce590b8095f8d4e24
    with:
      name: CNeuromod Bot
      email: courtois.neuromod@gmail.com
    secrets:
      ssh_key: ${{ secrets.CNEUROMOD_BOT_SSH_KEY }}
