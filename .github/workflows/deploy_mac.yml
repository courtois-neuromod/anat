name: deploy on macOS
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  deploy:
    uses: courtois-neuromod/actions-template/.github/workflows/deploy_mac.yml@4f5d2de082cdbdd7cf0aaabe83d1f681e9420afa
    with:
      name: CNeuromod Bot
      email: courtois.neuromod@gmail.com
    secrets:
      ssh_key: ${{ secrets.CNEUROMOD_BOT_SSH_KEY }}
